# Environment variables for local development
# Vite requires variables that should be exposed to the client to be prefixed with VITE_

# Upload endpoint used by FileUpload component (override to your API in real deployments)
VITE_UPLOAD_URL=https://httpbin.org/post

# Optional: provide a static token for development (prefer runtime/session tokens in production)
# VITE_UPLOAD_TOKEN=

# API base for other requests
VITE_API_BASE=https://api.example.com

# Optional Sentry DSN for error tracking
VITE_SENTRY_DSN=
