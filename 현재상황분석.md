먼저 프론트엔드와 백엔드 인증 관련 파일을 전방위로 탐색할게요.

이제 전체 인증 흐름을 파악했습니다. 종합 분석 자료를 작성할게요.

이제 모든 파일을 다 읽었습니다. 종합 분석 자료를 작성합니다.

---

# smep 인증 시스템 전체 분석 보고서

## 1. 현재 인증 아키텍처 전체 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                        smep-ufe (Frontend)                          │
│                                                                     │
│  Login.jsx           → POST /api/v1/auth/login (ID/PW)             │
│  SSOLogin.jsx        → MOCK (API 호출 없음, 더미 데이터)            │
│  keycloakGetAuthCode → Redirect to OnePass/Keycloak auth URL        │
│  OnePassSsoLogout    → Local store clear + navigate('/')            │
│  useAuthStore        → Zustand + sessionStorage persist             │
│  AuthContext         → Zustand store를 한번 더 감싼 레이어          │
│                                                                     │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ Bearer JWT
┌───────────────────────────▼─────────────────────────────────────────┐
│                        smep-be (Backend)                            │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  AuthController         /api/v1/account/login  (ID/PW 로그인)│   │
│  │  AccountContextController /api/v1/auth/login   (같은 로직!)  │   │
│  │  KeycloakController     /api/v1/auth/keycloak/*              │   │
│  │  AccountMeController    /api/v1/account/me                   │   │
│  │  SsoAuthController      /api/v1/auth/*  (@local Profile only)│   │
│  │  MockAuthController     /api/v1/auth/*  (@local Profile only)│   │
│  │  OidcController         /api/me         (oauth2Login 비활성)  │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  SecurityConfig: STATELESS + JwtAuthenticationFilter               │
│  BUT KeycloakController uses HttpSession for tokens !!             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. 로그인 플로우별 실제 코드 추적

### 2-1. 일반 ID/PW 로그인 (Login.jsx)

```
[Frontend Login.jsx]
  ① POST /api/v1/auth/login  { id, password, type: INDIVIDUAL|CORPORATE }
  ② GET  /api/v1/account/me  (accessToken으로)
  ③ useAuthStore.login({ token, refreshToken, profile })
  ④ navigate('/')

[Backend AccountContextController → AccountContextService]
  ① type 분기 (INDIVIDUAL / CORPORATE)
  ② useLegacyAccountStore() 체크:
      - H2 DB (로컬): 평문 비밀번호 비교 (!!!)
      - 실 DB: MemberLoginMapper.findActiveLoginCredential() + 비밀번호 해시 검증
  ③ AccountJwtProvider.createRealMemberToken() → access JWT
  ④ RefreshTokenRepository.save() → refresh JWT (DB 저장)
  ⑤ TokenResponse { accessToken, refreshToken } 반환
```

### 2-2. OnePass SSO 로그인 (OIDC Authorization Code Flow)

```
[Frontend keycloakGetAuthCode.onePassGetAuthCode()]
  ① window.location.href = Keycloak auth URL
     ?client_id=CLIENT_ID
     &redirect_uri=REDIRECT_SSO_URI (우리 콜백 페이지)
     &response_type=code
     &scope=openid
     (state 파라미터 없음 - 주석 처리됨)

[OnePass/Keycloak]
  ② 인증 완료 → redirect_uri로 code 전달

[Frontend SSO 콜백 페이지]
  ③ POST /api/v1/auth/keycloak/callback/local-login { code }

[Backend KeycloakController → KeycloakCallbackLocalLoginService]
  ④ KeycloakTokenService.exchangeCodeForToken(code)
     → POST to Keycloak /token endpoint
     → KeycloakTokenResponse { access_token, refresh_token, id_token }
  ⑤ session.setAttribute("keycloak_access_token", ...)
     session.setAttribute("keycloak_id_token", ...)  ← STATELESS 선언인데 session 사용!
  ⑥ KeycloakLocalLoginService.loginFromAccessToken(accessToken)
     → UUID 추출 → MemberLoginMapper.findIndividualByUuid(uuid)
     → AccountContextService.issueRealMemberLogin(member)
  ⑦ local TokenResponse 반환 (우리 서비스 JWT)

[Frontend]
  ⑧ useAuthStore.ssoLogin({ token, profile })
  ⑨ navigate('/')
```

### 2-3. 회원가입 (OnePass 연동)

```
[Frontend Login.jsx 회원가입 버튼]
  buildOnePassRegisterUrl(type='member'|'business')
  → window.location.href = ONEPASS_JOIN_URL
    ?type=member
    &return_client=CLIENT_ID
    &return_uri=REDIRECT_HOME_URI (메인 페이지!)

[OnePass에서 가입 완료 후]
  → REDIRECT_HOME_URI (메인 페이지)로 리다이렉트
  → 별도 로그인 단계 없음 (가입 후 자동로그인 X)
  → 사용자가 다시 로그인해야 함
```

### 2-4. 통합회원 전환 (intgMbrSwtcYn)

```
[Frontend OnepassLoginConversionModal.jsx]
  "중기 통합회원 전환" 버튼
  → onePassJoin()
    → resolveOnePassJoinMemberId(): user.loginId || user.id
    → resolveOnePassJoinUserType(): CORPORATE→'ENT', INDIVIDUAL→'IND'
    → KEYCLOAK_JOIN?redirect_uri=메인&mbrId=loginId&userType=ENT/IND&return_client=CLIENT_ID
  → window.location.href로 리다이렉트

[전환 완료 후]
  → REDIRECT_HOME_URI (메인 페이지)로 리다이렉트
  → 백엔드에서 intgMbrSwtcYn 업데이트 없음
  → 전환 완료 여부를 우리 DB에 반영하는 로직 없음
```

### 2-5. 로그아웃

```
[Frontend - KeycloakController 호출 방식]
  ① POST /api/v1/auth/keycloak/logout
  ② Backend: session에서 id_token 읽기 → Keycloak logout URL 조립
  ③ Frontend: logoutUrl로 window.location.href redirect
  ④ OnePass logout 완료 → post_logout_redirect_uri 으로 redirect
  ⑤ OnePassSsoLogout 컴포넌트:
     - sessionStorage.removeItem('keycloak_state')
     - useAuthStore.getState().logout() (Zustand 초기화)
     - navigate('/', { replace: true })

[Frontend - 로컬 로그아웃만 하는 경우]
  useAuthStore.logout() 직접 호출
  → BroadcastChannel로 다른 탭에 LOGOUT 전파
  → sessionStorage 'auth-storage' 삭제
  → OnePass 세션은 그대로 남음!
```

---

## 3. 발견된 문제점 목록

### 🔴 Critical (보안 위협)

#### #1. OIDC state 파라미터 비활성화 → CSRF 공격 취약

```javascript
// keycloakGetAuthCode.js
// 임시 연동 계약: 외부 출발 콜백과 맞추기 위해 state 저장과 auth URL state 전달을 중단한다.
// const state = crypto.randomUUID();
// sessionStorage.setItem('keycloak_state', state);

let params = new URLSearchParams({
  client_id: CLIENT_ID,
  redirect_uri: REDIRECT_SSO_URI,
  response_type: 'code',
  scope: 'openid',
  // state: state,  ← 완전히 제거됨
});
```

**문제**: Authorization Code Flow에서 `state`는 CSRF 방지의 핵심. 공격자가 조작된 `code`를 피해자의 redirect_uri로 주입하는 CSRF 공격이 가능.

**원인**: "외부 출발 콜백과 맞추기 위해" 라는 주석으로 보아 OnePass 측과 협의 없이 state 검증을 포기한 것으로 보임.

---

#### #2. JWT 토큰을 sessionStorage에 저장 → XSS 취약

```javascript
// useAuthStore.jsx
persist(
  ...,
  {
    name: 'auth-storage',
    storage: createJSONStorage(() => sessionStorage), // sessionStorage 사용
  }
)
```

**문제**: XSS 공격 시 `sessionStorage['auth-storage']`에서 access token + refresh token 탈취 가능. OWASP Top 10 취약점.

**올바른 방법**: HttpOnly Cookie + SameSite=Strict 또는 Lax.

---

#### #3. 전체 API 허용 (인증 미적용)

```java
// SecurityConfig.java
.requestMatchers("/api/v1/**").permitAll() // 전체 URL 오픈 (개발완료 및 운영반영시 수정)
```

**문제**: 개발 편의를 위해 열어둔 전체 허용이 운영에 그대로 반영될 가능성. JwtAuthenticationFilter는 동작하지만 Spring Security의 `authenticated()` 강제가 없으므로 토큰 없이도 대부분 API 접근 가능.

---

#### #4. 로컬 개발 환경에서 평문 비밀번호 비교

```java
// AccountContextService.java
private TokenResponse loginIndividualLegacy(ContextLoginRequest request) {
    AccountUser user = accountUserMapper.findByLoginId(request.getId());
    if (user == null || user.getPassword() == null
        || !user.getPassword().equals(request.getPassword())) {  // ← 평문 비교!
```

**문제**: H2(로컬)에서 비밀번호가 평문 저장 + 평문 비교됨. 개발자 PC가 해킹되거나 DB dump가 유출되면 바로 노출.

---

#### #5. SsoAuthController와 MockAuthController 중복 + 동일 경로

```java
// SsoAuthController.java  @Profile("local")  @RequestMapping("/api/v1/auth")
// MockAuthController.java  @Profile("local")  @RequestMapping("/api/v1/auth")
// 두 클래스가 완전히 동일한 endpoints를 가짐 → RequestMappingConflict 발생 가능
```

**문제**: 두 컨트롤러가 `local` 프로파일에서 동일 경로(`GET /api/v1/auth/login-url`, `GET /api/v1/auth/callback`)를 선언. Spring이 기동 중 충돌 에러를 내거나 임의로 하나를 선택.

---

### 🟠 High (기능 오작동)

#### #6. OidcController: oauth2Login 비활성화로 항상 401

```java
// SecurityConfig.java
// .oauth2Login(oauth -> oauth  ← 완전히 주석 처리됨
//     .loginPage("/oauth2/authorization/smes-tipa")
//     .defaultSuccessUrl("/login/success", true)
// )

// OidcController.java
@GetMapping("/api/me")
public ResponseEntity<?> me(@AuthenticationPrincipal OidcUser user) {
    if (user == null) {
        return ResponseEntity.status(401).build();  // 항상 여기로 떨어짐
    }
```

**문제**: `oauth2Login`이 비활성화 상태이므로 `@AuthenticationPrincipal OidcUser`는 항상 null. `/api/me` endpoint는 사실상 사용 불가.

---

#### #7. SecurityConfig STATELESS 선언 + HttpSession 사용 모순

```java
// SecurityConfig.java
.sessionManagement(session -> session.sessionCreationPolicy(STATELESS))

// KeycloakController.java
session.setAttribute("keycloak_access_token", tokens.getAccessToken());
session.setAttribute("keycloak_refresh_token", tokens.getRefreshToken());
session.setAttribute("keycloak_id_token", tokens.getIdToken());
```

**문제**: STATELESS 정책과 HttpSession 사용이 공존.
- Kubernetes 환경에서 Pod가 여러 개일 경우 세션 공유가 없으면 로그아웃 URL 생성 실패
- STATELESS 설정이 실제로 세션 생성을 막는 것이 아니라 Spring Security가 세션을 인증에 *사용*하지 않는다는 의미이므로 세션 자체는 생성됨 - 혼란 야기

---

#### #8. 두 개의 로그인 엔드포인트 중복

```java
// AuthController.java → /api/v1/account/login
// AccountContextController.java → /api/v1/auth/login
// 둘 다 AccountContextService.login()을 호출하는데:

// AuthController
ContextLoginRequest request = new ContextLoginRequest();
request.setType(USER_TYPE_INDIVIDUAL);  // ← 항상 INDIVIDUAL 고정!

// AccountContextController
// request.type을 그대로 사용 (INDIVIDUAL/CORPORATE 구분 가능)
```

**문제**: `AuthController.login()`은 type을 항상 `INDIVIDUAL`로 고정해서 기업 회원이 이 엔드포인트로 호출하면 로그인 실패. `Login.jsx`는 `AccountContextController`의 `/api/v1/auth/login`을 사용하므로 `AuthController`의 `/api/v1/account/login`은 dead code.

---

#### #9. AuthService.login()은 항상 더미 토큰 반환

```java
// AuthServiceImpl.java
@Override
public TokenResponse login(LoginRequest loginRequest) {
    // TODO: Keycloak과 연동하여 실제 인증을 처리하고 JWT를 발급받는 로직 구현
    return new TokenResponse("dummy-access-token", "dummy-refresh-token");  // ← 항상 더미!
}
```

**문제**: `AuthService`가 인터페이스로 정의되어 있고 실제 구현은 더미. `AuthController.login()`이 내부적으로 `AccountContextService.login()`을 호출하므로 실제로는 실행되지 않지만, 테스트나 다른 주입 경로에서 `AuthService`를 직접 사용하면 항상 더미 토큰이 발급됨.

---

#### #10. 통합회원 전환 후 DB 업데이트 없음

```javascript
// onePassJoin() → 메인 페이지로 redirect
// 전환 완료 신호를 받는 콜백 없음
// intgMbrSwtcYn 필드를 업데이트하는 API 호출 없음
```

```java
// AccountMeController → buildRealIndividualContext()에서 intgMbrSwtcYn 반환하지만
// 전환 완료 시 이를 업데이트하는 endpoint 없음
```

**문제**: OnePass에서 전환 완료 후 우리 서비스로 돌아왔을 때 전환 결과를 반영할 방법이 없음. 사용자는 통합회원 전환했지만 우리 DB의 `intgMbrSwtcYn`은 여전히 'N'.

---

#### #11. AuthContext의 이중 상태 관리 (버그 잠재)

```javascript
// AuthContext.jsx
export function AuthProvider({ children }) {
  const { token, user, isLogin, login: storeLogin } = useAuthStore();
  const [localUser, setLocalUser] = useState(null);    // ← 별도 state
  const [localToken, setLocalToken] = useState(null);  // ← 별도 state

  // isAuthenticated 로직
  isAuthenticated: !!(token || localToken || isLogin),
  // token=store, localToken=context local, isLogin=store
  // 세 곳 중 하나만 있어도 인증됨 → 안전하지 않은 판단
```

**문제**: `useAuthStore`의 state와 `AuthContext` 로컬 state가 분리 운영. `storeLogout()`만 호출하면 `localToken`은 남아있어 `isAuthenticated`가 여전히 true가 됨. 실제 `Login.jsx`는 `useAuthStore`를 직접 사용해 `AuthContext.login()`을 우회함.

---

### 🟡 Medium (설계 결함)

#### #12. PKCE(Proof Key for Code Exchange) 미구현

```javascript
// keycloakGetAuthCode.js
let params = new URLSearchParams({
  client_id: CLIENT_ID,
  redirect_uri: REDIRECT_SSO_URI,
  response_type: 'code',
  scope: 'openid',
  // code_challenge 없음
  // code_challenge_method 없음
});
```

**문제**: Public client(SPA)에서 Authorization Code Flow 사용 시 PKCE는 RFC 7636 / OAuth 2.1에서 필수. Authorization Code Interception Attack 취약.

---

#### #13. Refresh Token 만료 시간 관리 부재

```java
// RefreshToken.java - DB 엔티티
// userId, token, userType 필드만 있음
// 만료일시(expiresAt) 컬럼 없음
```

```java
// AccountContextService.refreshToken()
if (!accountJwtProvider.validateToken(refreshToken)) {
    throw new BusinessException(CommonErrorCode.UNAUTHORIZED, "Invalid Refresh Token");
}
// JWT 자체 만료는 검증하지만 DB에서 무효화(revoke)된 토큰 구분 불가
```

**문제**: Refresh Token을 DB에 저장하면서도 만료 시간 컬럼이 없어 만료 여부를 JWT 서명에만 의존. 도난된 refresh token을 서버 측에서 강제 무효화(revoke) 불가.

---

#### #14. 토큰 만료 시 자동 갱신 로직 없음 (Frontend)

```javascript
// apiClient.js - Fetch 기반
// 401 응답 시 refresh token으로 재발급하는 인터셉터 없음
// 토큰 만료 시 API 오류만 발생, 자동 재로그인 없음

// http.js - Axios 기반 (http.js와 apiClient.js 두 클라이언트 공존)
const http = axios.create({ baseURL: appBaseUrl });
// Axios interceptor 없음 - 401 처리 없음
```

**문제**: 사용자가 access token 만료 후 작업 시 갑작스러운 에러 발생. UX 불량 + 두 개의 HTTP 클라이언트(`apiClient.js`의 Fetch, `http.js`의 Axios)가 공존해서 어느 것이 실제 사용되는지 혼란.

---

#### #15. `id` 저장 체크박스 기능 미구현

```jsx
// Login.jsx
<input type="checkbox" name="save_id" id="save_id"/>
<label htmlFor="save_id">아이디 저장</label>
// onChange 핸들러 없음, 저장 로직 없음
```

**문제**: UI에는 있지만 기능 없음. 사용자 혼란.

---

#### #16. SSOLogin.jsx: 완전한 Mock (실제 운영 불가)

```javascript
// SSOLogin.jsx handleClick()
// const response = await apiClient.post('/api/v1/account/scenario-login', body);
const companyProfile = getCompanyProfileByBizNo(brno);  // 로컬 더미 데이터
login({ profile: companyProfile });
window.opener.postMessage({ type: 'LOGIN_SUCCESS', ... }, origin);
window.close();
```

**문제**: 실제 API 호출 없이 로컬 더미 데이터로 로그인 처리. 비밀번호 입력 필드가 있지만 전혀 사용 안됨. 운영 환경에서는 절대 사용 불가.

---

#### #17. AuthController.getProfile()의 위험한 fallback

```java
// AuthController.java
private ScenarioUser getUserFromToken(String authHeader) {
    // 토큰 파싱 실패, brno 없는 경우 기본값(유큐브) 반환
    return authService.getScenarioUser("2288105280");  // ← 항상 유큐브 반환
}
```

**문제**: 토큰이 없거나 파싱 실패해도 특정 회사(유큐브)의 데이터를 반환. 미인증 사용자가 유큐브 사용자로 오인될 수 있음. 명백한 시연용 코드가 production 코드에 남아있음.

---

#### #18. id_token 서명 검증 선택적 적용

```java
// StandardSsoClient.java
public Map<String, Object> parseIdToken(String idToken) {
    if (ssoProperties.getJwtSecret() != null && !ssoProperties.getJwtSecret().isBlank()) {
        return parseAndVerifyIdToken(idToken);  // 서명 검증
    }
    // jwtSecret이 없으면 Base64 디코딩만 (서명 검증 없음!)
    String payload = new String(Base64.getUrlDecoder().decode(parts[1]), StandardCharsets.UTF_8);
    return OBJECT_MAPPER.readValue(payload, Map.class);
}
```

**문제**: `sso.jwt-secret` 설정이 없으면 id_token 서명을 검증하지 않고 payload만 디코딩. 조작된 id_token으로 인증 우회 가능.

---

#### #19. 회원 구분 용어 혼재

| 위치 | 개인회원 | 기업회원 |
|------|---------|---------|
| Login.jsx | `INDIVIDUAL` | `CORPORATE` |
| AccountContextService | `IND` | `ENT` |
| keycloakGetAuthCode.js (OnePass 전환) | `IND` | `ENT` |
| MemberLoginIdentity | `memberTypeCode` |  |
| AccountMeController 분기 | `USER_TYPE_CORPORATE` | `USER_TYPE_INDIVIDUAL` |
| Token claims `type` | `INDIVIDUAL` | `CORPORATE` |

**문제**: `INDIVIDUAL/CORPORATE`, `IND/ENT`, 단순 문자열이 혼재. 변환 로직 곳곳에 산재. 단일 enum 정의 없음.

---

#### #20. 기업 회원 context 미완성 (switch-context)

```java
// AccountContextController
@PostMapping("/switch-context")
// 개인회원이 여러 기업에 소속될 때 컨텍스트 전환용
// 단, 기업 회원(loginCorporate)으로 로그인한 경우 context_company_id가 null로 발급됨
```

```java
// AccountContextService.issueRealMemberTokenPair()
if (MEMBER_TYPE_CODE_CORPORATE.equalsIgnoreCase(memberTypeCode)) {
    AccountTokenContext context = AccountTokenContext.corporate(null, ROLE_OWNER, roles);
    // ↑ companyId가 null! 기업 회원 토큰에 company 정보 없음
```

**문제**: 기업 회원 로그인 시 JWT에 `context_company_id`가 null로 발급됨. `/api/v1/account/me`에서 `buildRealCorporateContext()`를 타지만 company 정보 조회가 불완전할 수 있음.

---

## 4. OIDC/OAuth 2.0 표준 준수 분석

```
OAuth 2.0 Authorization Code Flow 체크리스트:

✅ Authorization Code Flow 사용
✅ code → token 교환 (서버 사이드)
✅ client_secret 사용
✅ scope=openid 포함
✅ id_token 처리
✅ userinfo endpoint 호출

❌ state 파라미터 (CSRF 방지) - 비활성화됨
❌ PKCE (code_challenge/code_verifier) - 미구현
❌ nonce 검증 - 없음
❌ aud (audience) 검증 - StandardSsoClient에서 미검증
❌ iss (issuer) 검증 - jwtSecret 없으면 미검증
❌ iat/exp 검증 - parseIdToken에서 서명 없이 Base64만 디코딩
❌ token_type_hint (revocation) - 없음
❌ PKCE S256 method - 없음
❌ 로그아웃: post_logout_redirect_uri → 구현되어 있으나 세션 의존
```

**결론**: Authorization Code Flow의 외형은 갖추고 있으나 핵심 보안 요소(state, PKCE, 검증)가 빠져있어 표준 OIDC라고 보기 어려움.

---

## 5. 각 회원 유형별 플로우 문제

### 개인 회원 (INDIVIDUAL)

```
[정상 플로우]
Login.jsx → POST /api/v1/auth/login { type: INDIVIDUAL }
→ AccountContextService.loginIndividual()
→ MemberLoginMapper.findActiveLoginCredential()
→ 비밀번호 해시 검증
→ JWT 발급 (auth_source=REAL_MEMBER_DB)
→ /api/v1/account/me → buildRealIndividualContext()

[문제]
- additionalInfoRequired=true인 경우 추가정보 입력 UI로 분기 로직이 있으나
  프론트엔드에서 이를 처리하는 페이지/컴포넌트가 확인되지 않음
- intgMbrSwtcYn=N인 개인 회원에게 전환 모달(OnepassLoginConversionModal)을 언제 보여주는지
  트리거 로직이 명확하지 않음
```

### 기업 회원 (CORPORATE)

```
[정상 플로우]
Login.jsx → POST /api/v1/auth/login { type: CORPORATE }
→ AccountContextService.loginCorporate()
→ MemberLoginMapper.findCorporateByLoginIdAndPassword()
→ JWT 발급 (type=ENT, auth_source=REAL_MEMBER_DB, context_company_id=null!)

[문제 #1] context_company_id null 발급
  → /api/v1/account/me → buildRealCorporateContext(memberNo)
  → CorporateMemberRepository.selectDetailById(memberNo) 로 조회
  → contextCompanyId = parseLongClaim(memberNo) ← memberNo(문자열)를 Long으로 파싱!
    실제 company_id가 아닌 mbrNo를 companyId로 사용하는 오류

[문제 #2] 기업 회원과 개인 회원 모두 linked company를 가질 수 있는 구조이지만
  기업 회원 로그인 시 linkedCompanies는 항상 빈 배열
```

### 마이비즈니스 대시보드

```
[인증 관련]
MyBusinessDashBoard.jsx → useAuthStore에서 bizno, cmpNm, companySize 사용
→ isLogin 체크만 하고 권한(contextRole) 체크 없음
→ INDIVIDUAL 모드로 로그인한 사용자도 URL 직접 접근 시 대시보드 진입 가능

[데이터 조회]
bizno로 기업 데이터 조회 → bizno가 null이면 API 오류
→ 개인 회원이 진입하면 bizno=null → 오류
```

---

## 6. 개선 우선순위 로드맵

### Phase 1 - 즉시 수정 (보안)

| 우선순위 | 항목 | 방법 |
|---------|------|------|
| P0 | state 파라미터 복원 | Redis SsoStateStore가 이미 있음 → 프론트에서 state 생성 후 전송 |
| P0 | `/api/v1/**` permitAll 제거 | 인증 필요 API 목록 정의 후 `authenticated()` 적용 |
| P1 | id_token 서명 검증 필수화 | sso.jwt-secret 없으면 기동 실패하도록 |
| P1 | SsoAuthController, MockAuthController 중복 제거 | 하나로 합치거나 조건부 Bean |
| P1 | AuthController.getProfile() 더미 fallback 제거 | 401 반환으로 교체 |

### Phase 2 - 단기 개선 (기능 정합)

| 우선순위 | 항목 | 방법 |
|---------|------|------|
| P1 | 기업 회원 context_company_id null 수정 | loginCorporate 시 회사 조회 후 token에 포함 |
| P1 | AuthService dummy token 제거 | AuthController에서 AccountContextService로 직접 위임 |
| P2 | Refresh Token 만료일시 추가 | RefreshToken 엔티티에 expires_at 컬럼 |
| P2 | 401 인터셉터 추가 | apiClient.js에서 401 → refresh → 재시도 |
| P2 | 통합회원 전환 완료 콜백 | OnePass 전환 후 return_uri에 /callback 경로 지정, BE에서 intgMbrSwtcYn 업데이트 |
| P2 | http.js, apiClient.js 통합 | Axios 인터셉터 기반 단일 클라이언트 |

### Phase 3 - 중기 개선 (OIDC 표준화)

| 우선순위 | 항목 | 방법 |
|---------|------|------|
| P2 | PKCE 도입 | code_verifier 생성 → sessionStorage → code_challenge 전송 |
| P2 | Token을 HttpOnly Cookie로 이전 | BE에서 Set-Cookie, FE에서 저장 로직 제거 |
| P3 | 회원 유형 enum 통일 | MemberType.INDIVIDUAL('IND'), CORPORATE('ENT') 단일 정의 |
| P3 | STATELESS 정책 일관성 | HttpSession 제거 → Redis로 keycloak token 관리 (이미 Redis 사용 중) |
| P3 | OidcController 활성화 또는 제거 | oauth2Login 활성화 or endpoint 삭제 |

---

## 7. 실제 정상 운영을 위한 최소 조건

```
현재 실제로 동작하는 경로:
✅ Login.jsx → /api/v1/auth/login (ID/PW, 실 DB 연결 시)
✅ onePassGetAuthCode → OnePass → /api/v1/auth/keycloak/callback/local-login (SSO)
✅ Logout → /api/v1/auth/keycloak/logout → OnePass logout → OnePassSsoLogout

현재 동작하지 않거나 Mock인 경로:
❌ SSOLogin.jsx - 완전한 Mock, 운영 불가
❌ OidcController /api/me - oauth2Login 비활성화로 항상 401
❌ AuthController /api/v1/account/login - type 고정 버그
❌ AuthService.login() - 더미 토큰 반환
❌ 통합회원 전환 완료 시 DB 업데이트

운영 배포 전 최소 필수 작업:
① state 파라미터 복원 (CSRF 방지)
② /api/v1/** permitAll 수정
③ SSOLogin.jsx 실제 SSO 연동으로 교체 또는 삭제
④ AuthController.getProfile() 더미 fallback 제거
⑤ id_token 서명 검증 필수화
```

---

분석을 정리하면, **전반적으로 OIDC Authorization Code Flow의 외형은 갖추고 있으나** state 비활성화, PKCE 부재, 서명 검증 누락 등 핵심 보안 요소가 빠진 상태입니다. 또한 **Mock/더미 코드와 실제 코드가 혼재**되어 있어서 어느 경로가 실제 동작하는지 파악하기 어려운 구조입니다. 그리고 **두 개의 프레임워크(Fetch 기반 apiClient + Axios 기반 http)와 두 개의 Auth Store(Zustand store + AuthContext local state)**가 이중으로 존재해서 유지보수 부담이 큽니다.